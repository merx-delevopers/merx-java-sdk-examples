quarkus.http.port=8081

# Token de integracao - vem da variavel de ambiente MERX_API_KEY.
merx.api-key=${MERX_API_KEY:}
# SANDBOX (default) ou PRODUCTION.
merx.environment=${MERX_ENV:SANDBOX}

# Logs
quarkus.log.category."com.cooperativa.demo".level=INFO
quarkus.log.category."com.merx.sdk".level=WARN
